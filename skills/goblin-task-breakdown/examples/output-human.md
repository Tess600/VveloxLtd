# Goal

Create a portable task-breakdown skill.

# Done means

`SKILL.md` validates and one example graph passes the published schema.

# Do now

Open the repository's `skills` directory in the editor. Stop when the directory is visible.

# Next

1. Create the `goblin-task-breakdown` folder.
2. Create `SKILL.md` inside it.
3. Add the required `name` and `description` frontmatter.

# Blocked or waiting

None.

# Later

Add export adapters after the canonical graph is stable.

